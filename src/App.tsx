import React from 'react';
import { Cell } from './components/Cell/Cell';

const App: React.FC = () => {
    return (
        <div>
            <Cell color={3} />
        </div>
    );
};

export default App;